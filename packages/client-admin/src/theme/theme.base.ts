import {ThemeOptions} from '@mui/material';

export const themeBase: Partial<ThemeOptions> = {
  shape: {
    borderRadius: 10
  },
  typography: {
    button: {
      textTransform: 'none'
    }
  }
};
