query getProject($id: String!) {
  getProject(id: $id) {
    id
    name
    logo
    muiTheme
    redirectUrl
    settings {
      displayProjectName
      allowSignup
      verifyEmail
    }
    authMethods {
      googleAuth
      emailAuth
    }
  }
}
